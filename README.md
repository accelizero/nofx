# 🤖 NOFX - 下一代AI驱动的加密货币期货自动交易系统

[![Go Version](https://img.shields.io/badge/Go-1.21+-00ADD8?style=flat&logo=go)](https://golang.org/)
[![React](https://img.shields.io/badge/React-18+-61DAFB?style=flat&logo=react)](https://reactjs.org/)
[![TypeScript](https://img.shields.io/badge/TypeScript-5.0+-3178C6?style=flat&logo=typescript)](https://www.typescriptlang.org/)
[![License](https://img.shields.io/badge/License-MIT-green.svg)](LICENSE)

---

这个项目已经被我魔改得面目全非了，也亏麻了，原版README去原项目看吧，说点不一样的。

前端开放观摩: [https://nofx.justodo.me](https://nofx.justodo.me)

## ✨ 本项目的特别之处


### 1. 📝 **完整的交易逻辑追踪系统**

**原项目的问题：**
- 只知道"开了多单"或"平了空单"
- 不知道"为什么开"、"为什么平"
- 无法复盘分析交易逻辑的有效性

**本项目的解决方案：**

#### 1.1 持仓逻辑记录
每次开仓时，系统自动记录：
- **进场逻辑**：AI的完整推理过程（为什么在这个价位开仓）
- **出场逻辑**：AI规划的出场条件（什么情况下平仓）
- **多时间框架逻辑**：基于哪个时间框架的趋势判断
- **时间戳**：记录逻辑的时间，便于追踪

#### 1.2 逻辑有效性检查
系统会实时检查持仓逻辑是否仍然有效：
```go
// 检查逻辑是否失效
invalid, reasons := CheckLogicValidity(logic, symbol, marketData, ctx, side)
// 返回：是否失效 + 失效原因列表
```

**检查内容：**
- ✅ 多时间框架趋势是否发生变化
- ✅ 进场逻辑的前提条件是否仍然满足
- ✅ 出场逻辑的触发条件是否已满足

**前端展示：**
- 每个持仓都有"逻辑"按钮
- 点击可查看完整的进场/出场逻辑
- 如果逻辑失效，会显示失效原因

#### 1.3 历史成交逻辑记录
每笔交易完成后，系统记录：
- **进场逻辑**：开仓时的推理
- **出场逻辑**：开仓时规划的出场条件
- **平仓原因**：实际平仓的理由（AI决策/止损/强制平仓）
- **强制平仓逻辑**：如果是强制平仓，记录原因

**前端展示：**
- 历史成交记录中每条都有"逻辑"按钮
- 可以查看完整的交易逻辑链条
- 支持复盘分析，了解哪些逻辑有效、哪些失效

---

### 2. 🎯 **可配置的策略系统**

**原项目的问题：**
- 策略硬编码在代码中
- 修改策略需要改代码、重新编译
- 无法快速切换不同策略

**本项目的解决方案：**
```toml
[strategy]
  name = "base_prompt"  # 策略文件名（strategies/base_prompt.txt）
  preference = "aggressive"  # 策略偏好：conservative/aggressive/balanced
```

**工作原理：**
1. 策略提示词存储在独立的文本文件中（`strategies/*.txt`）
2. 系统启动时动态加载策略文件
3. 可以配置策略偏好（保守/激进/平衡）
4. 支持多策略切换，无需修改代码

**优势：**
- ✅ 策略与代码分离，易于维护
- ✅ 支持A/B测试不同策略
- ✅ 可以针对不同市场环境使用不同策略
- ✅ 非技术人员也可以修改策略提示词

---

## ⚙️ 配置说明

### 基础配置

```toml
# Trader配置
[[traders]]
  id = "my_trader"
  name = "我的AI交易员"
  enabled = true
  ai_model = "deepseek"  # deepseek / qwen / custom
  exchange = "aster"      # binance / hyperliquid / aster
  
  # Aster配置（示例）
  aster_user = "0x..."
  aster_signer = "0x..."
  aster_private_key = "0x..."
  
  # AI API密钥
  deepseek_key = "sk-..."
  
  # 初始余额
  initial_balance = 1000.0
  
  # 扫描间隔（分钟）
  scan_interval_minutes = 5

# 杠杆配置
[leverage]
  btc_eth_leverage = 20
  altcoin_leverage = 10

# 风险控制
max_daily_loss = 50.0
max_drawdown = 50.0
position_stop_loss_pct = 10.0

# 策略配置
[strategy]
  name = "base_prompt"
  preference = "aggressive"  # conservative / aggressive / balanced

# 多时间框架分析
[analysis_mode]
  mode = "multi_timeframe"
  
  [analysis_mode.multi_timeframe]
    [analysis_mode.multi_timeframe.weights]
      daily = 0.4
      hourly4 = 0.3
      hourly1 = 0.2
      minute15 = 0.1
    
    min_consistency_score = 0.5
    enable_cache = true
```